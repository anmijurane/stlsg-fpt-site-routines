import type JSConfetti from 'js-confetti';
import React, { useEffect, useRef } from 'react'

export const ConfettiDispatch = () => {

  const confettiRef = useRef<JSConfetti>(null);

  useEffect(() => { }, [])

  return null;
}
